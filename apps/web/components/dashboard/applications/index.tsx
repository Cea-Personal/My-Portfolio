export function ApplicationWorkspace() {
  return (
    <section aria-labelledby="application-title">
      <h2 id="application-title">Application workspace</h2>
      <p>Draft, review, finalize, and preserve a package without submitting externally.</p>
    </section>
  );
}
