# Drive sync runbook

Verify the connection owner, rotate the channel before expiry, and replay the durable outbox event.
Permission loss marks the source unavailable; it never deletes canonical facts automatically.
