# Document corpus

The worker test corpus covers PDF, DOCX, and text parsing, signature/size checks, zip-bomb rejection,
quarantine, chunk hashes, and idempotent embedding behavior. A larger 100-document corpus can be
mounted in CI without changing the parser limits.
