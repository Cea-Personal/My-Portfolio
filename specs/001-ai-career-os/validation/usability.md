# Usability protocol

The public page provides a skip link, semantic section landmarks, keyboard navigation, reduced-motion
CSS, and explicit unavailable states for AI. Owner workflows expose review/confirmation boundaries.
Run `pnpm test:e2e` with seeded fixtures to record participant timing and first-attempt completion.
