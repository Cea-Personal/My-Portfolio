# Data Model: AI Career OS and Intelligent Portfolio

**Date**: 2026-08-31

**Reconciled**: 2026-09-02

**Database**: PostgreSQL 17 with pgvector 0.8.6

**Authority**: `spec.md` requirements FR-001–FR-136, ST-001–ST-007

## Modeling Conventions

- Primary keys are UUIDs generated by the database. Foreign keys use explicit `ON DELETE` behavior.
- Owner-scoped records carry `owner_id` referencing the authenticated profile and are protected by RLS.
- Mutable records carry `created_at`, `updated_at`, and optimistic `revision` where concurrent edits matter.
- Historical or generated content is append-only; corrections create a new version or transition row.
- Times are `timestamptz` in UTC. User timezone is stored on the owner profile and schedule.
- Money uses `numeric` plus ISO 4217 currency; never binary floating point.
- Percentages and model confidences are constrained numeric values from 0 through 1.
- Enumerations are constrained text values so migrations can add states without destructive enum rewrites.
- Secrets are never stored directly in domain tables; tables retain only a secret-manager reference.
- Large binaries live in private object storage; database rows retain object key, size, media type, and hash.
- Untrusted raw provider payloads are retained only when permitted, encrypted/private, size-limited, and
  covered by retention policy.
- `app` holds private domain tables, `published` holds immutable public snapshots, and exposed `api`
  views/functions are allowlisted. All exposed relations use RLS or security-invoker semantics.

## Shared Value Sets

| Value set | Allowed values |
|-----------|----------------|
| `visibility` | `public`, `private`, `restricted` |
| `trust_level` | `verified_document`, `owner_verified`, `corroborated`, `ai_extracted_reviewed`, `ai_extracted`, `ai_inferred` |
| `review_status` | `candidate`, `in_review`, `approved`, `edited_approved`, `rejected`, `deferred` |
| `run_status` | `pending`, `running`, `completed`, `partial`, `failed`, `cancelled` |
| `source_availability` | `available`, `partial`, `unavailable` |
| `project_type` | `personal`, `open_source`, `professional` |
| `contribution_type` | `designed`, `built`, `led`, `co_built`, `contributed`, `maintained`, `migrated`, `optimized`, `architected` |
| `requirement_priority` | `required`, `preferred`, `optional` |
| `match_class` | `strong`, `related`, `partial`, `no_evidence` |
| `artifact_status` | `draft`, `owner_reviewed`, `final`, `submitted_snapshot`, `superseded` |
| `article_status` | `draft`, `scheduled`, `published`, `archived` |
| `interview_stage_status` | `proposed`, `manual`, `planned`, `scheduled`, `completed`, `cancelled`, `skipped` |

## Identity, Configuration, and Audit

### `app.owner_authorizations`

Server-managed authorization binding between the sole career owner and the authentication subject. It is
not writable through ordinary owner APIs.

| Field | Type | Rules |
|-------|------|-------|
| `auth_subject_id` | uuid | PK; equals the authorized authentication subject |
| `role` | text | constrained to `owner` in this release |
| `status` | text | `active` or `revoked`; one active owner enforced by unique singleton key |
| `provisioned_by`, `provisioned_at` | text/time | deployment or audited bootstrap provenance |
| `revoked_at`, `reason` | time/text | optional; no secret or credential content |

All private session establishment, profile creation, RLS ownership checks, workflow dispatch, and owner
exports require an active authorization row. A valid authenticated subject without one receives no
profile and no owner access. Changing the active owner is a separately reviewed administrative recovery
operation, not a self-service mutation.

### `app.profiles`

One row per authenticated owner.

| Field | Type | Rules |
|-------|------|-------|
| `id` | uuid | PK/FK to active owner authorization; equals authentication subject |
| `display_name` | text | required, 1–120 characters |
| `headline`, `bio`, `location` | text | private source values; public copies come from publication snapshots |
| `timezone` | text | required IANA timezone, default `Africa/Kigali` |
| `locale` | text | default `en` |
| `links` | jsonb | URL values validated against allowlisted schemes |
| audit fields | timestamps/revision | required |

### `app.application_profiles`

Reusable private deterministic application data. PK/FK `owner_id`; fields include email, telephone,
location, country, professional links, notice period, relocation, work authorization, sponsorship,
languages, education summary, and certification summary. Sensitive voluntary demographic fields are
intentionally absent. `revision` and `application_profile_versions` preserve changes; `archived_at`
removes a profile from active selectors without deleting audit or historical application context.

### `app.integration_connections`

| Field | Type | Rules |
|-------|------|-------|
| `id`, `owner_id` | uuid | PK/FK; unique owner + provider + external account |
| `provider`, `connection_type` | text | required, allowlisted adapter identifier |
| `external_account_id` | text | provider-scoped identity; encrypted if sensitive |
| `status` | text | `pending`, `active`, `error`, `revoked` |
| `scopes` | text[] | normalized granted scopes |
| `secret_ref` | text | server-only secret-manager reference |
| `cursor`, `cursor_version` | text | sync state; changed transactionally |
| `channel_id`, `channel_expires_at` | text/time | optional webhook channel metadata |
| `last_success_at`, `last_error_code` | time/text | no sensitive error body |

### `app.audit_events`

Append-only security and consequential-action history: `id`, `owner_id`, actor type/id, action, target
type/id, request/correlation ID, before/after metadata allowlist, reason, IP pseudonym, user-agent class,
and `occurred_at`. No document, prompt, answer, or credential text.

### `app.outbox_events`

Transactional handoff to Inngest: `id`, aggregate type/id, event name/version, JSON payload containing IDs
only, idempotency key, `available_at`, publish status, attempt count, and timestamps. Unique event name +
idempotency key prevents duplicate logical dispatch.

## Career Brain

### Structured Career Records

| Table | Purpose and key fields |
|-------|------------------------|
| `app.organizations` | Employer/client identity: owner, canonical name, public-safe name, industry, location, website, visibility. Unique normalized name per owner. |
| `app.career_experiences` | Organization, role title, career stage, employment type, start/end dates, current flag, private summary, approved public summary, display order, visibility. End date cannot precede start. |
| `app.skills` | Canonical skill/technology name, category, aliases, description, visibility. Unique normalized name per owner. |
| `app.experience_skills` | Experience-skill relationship with evidence strength, first/last used dates, and claim ID. Unique pair. |
| `app.projects` | Type, title, slug, organization/experience, source availability, visibility/confidentiality, private/public descriptions, contribution type, dates, links, and sanitized architecture reference. Slug unique per owner. |
| `app.project_skills` | Project-skill relationship with evidence strength and claim ID. Unique pair. |
| `app.achievements` | Experience/project, statement, action, outcome, business context, contribution type, visibility, and controlling claim ID. |
| `app.career_metrics` | Achievement/project, value, unit, direction, baseline, timeframe, context, attribution, display text, visibility, and controlling claim ID. Value and display text require evidence. |
| `app.education_records` | Institution, qualification, subject, start/end dates, status, visibility, and claim ID. |
| `app.certifications` | Issuer, name, issued/expiry dates, credential identifier/link, visibility, and claim ID. |
| `app.architecture_decisions` | Experience/project, title, context, decision, alternatives, outcome, sanitized public summary, visibility, and claim ID. |
| `app.leadership_examples` | Experience/project, situation, owner contribution, team context, outcome, visibility, and claim ID. |

### `app.career_facts`

Canonical claim identity.

| Field | Type | Rules |
|-------|------|-------|
| `id`, `owner_id` | uuid | PK/FK |
| `fact_type` | text | organization, role, date, responsibility, technology, project, leadership, skill, achievement, metric, certification, education, impact, decision |
| `subject_type`, `subject_id` | text/uuid | allowlisted polymorphic target; verified by deferred constraint trigger |
| `current_version_id` | uuid | FK to approved or current fact version |
| `trust_level` | text | independent of model confidence |
| `review_status` | text | follows ST-001 |
| `visibility` | text | defaults `private` |
| `verified_by_owner` | boolean | true only through owner review/manual workflow |
| `valid_from`, `valid_to` | date | optional; ordered when both present |
| `superseded_by_id` | uuid | nullable self-FK; no cycles |

### `app.career_fact_versions`

Append-only text and structured value history: `id`, fact ID, sequential version, statement, structured
value JSON, source type, model/extractor version where applicable, confidence, editor actor, edit reason,
content hash, and created time. Unique fact + version. Approved versions cannot be updated.

## Evidence and Knowledge

### `app.evidence_sources`

Stable source identity: owner, source type (`document`, `manual_fact`, `project_record`, `public_source`,
`article`), title, canonical URI, issuer/author, visibility, trust level, verification state, license or
use notes, availability, and retention class. Manual sources link to the relevant fact.

### `app.evidence_versions`

Immutable source version: evidence source, ordinal version, external revision metadata, raw object key,
raw SHA-256, normalized text SHA-256, media type, byte size, extractor/parser name and version,
processing timestamp, quarantine status, and prior version. Unique source + raw/content version key.

### `app.evidence_chunks`

| Field | Type | Rules |
|-------|------|-------|
| `id`, `evidence_version_id` | uuid | PK/FK; immutable |
| `ordinal` | integer | >= 0; unique per evidence version |
| `page_start`, `page_end` | integer | optional positive ordered pages |
| `section_path` | text[] | normalized headings |
| `char_start`, `char_end` | integer | offsets in normalized text; ordered |
| `content` | text | bounded normalized untrusted text |
| `content_hash` | text | SHA-256; unique within version/offset |
| `search_vector` | tsvector | generated weighted full-text content |
| `visibility`, `trust_level` | text | copied/enforced from source, may only become stricter |
| `deleted_at` | timestamptz | tombstone; excluded from retrieval |

Indexes: GIN on `search_vector`; B-tree on evidence version, visibility, trust, and tombstone fields.

### `app.chunk_embeddings`

Chunk, provider, model, model version, dimensions, embedding version, untyped `vector`, normalization,
input hash, status, and created time. Unique chunk + embedding version. Only compatible dimensions and
model versions share an exact or partial HNSW expression index.

### `app.claim_evidence`

Many-to-many support link: career fact version, evidence chunk/version, exact supporting offsets,
support class (`direct`, `transferable`, `related`, `weak`, `contradictory`), verification status,
verifier actor/run, rationale, and created time. Unique fact version + chunk + offset range.

### Documents and Ingestion

| Table | Purpose and key fields |
|-------|------------------------|
| `app.documents` | Evidence source, integration connection, external file ID/drive ID, name, source MIME, parent path, owner, availability, last seen version, removed/permission-lost time. Unique connection + external file ID. |
| `app.document_versions` | Evidence version, Drive version/head revision, export MIME, provider checksum, internal SHA-256, modification time, download status, and prior version. |
| `app.ingestion_runs` | Owner, trigger, connection, correlation/idempotency key, status, start/end, document counts, error summary, and workflow run ID. |
| `app.ingestion_items` | Run + document/version, stage, status, attempts, parser metrics, warnings, sanitized error, start/end. Unique run + document version. |
| `app.extracted_facts` | Ingestion item, original structured extraction, statement, subject candidate, confidence, trust, model/prompt/schema version, source offsets, and review status. |
| `app.fact_reviews` | Extracted fact, reviewer, decision, edited fact/version, reason, and time. Append-only; one terminal approval/rejection per extraction. |

## Portfolio Projection and Publication

### `app.portfolio_projection_rules`

Owner-controlled draft projection: source entity type/id, public eligibility, featured flag, priority,
career stage, section/category, display order, public summary override, metric selection, technology
selection, media selection, and last reviewer. Unique source entity per active projection revision.

Allowed public composition groups are `hero`, `about`, `experience`, `projects`, `blog`, and `contact`;
Ask Basil is an interactive shell bound to the publication rather than editable career content. Career
stage values use stable keys for `web_developer`, `software_engineer`, `lead_software_engineer`,
`data_engineer`, `senior_data_engineer`, and `ai_engineer_software_data`. Projection validation requires
professional projects, achievements, metrics, skills, and tools associated with a stage to publish inside
that Experience chapter. The AI stage carries `stage_kind = capability` unless evidence supports a formal
role.

### `published.portfolio_publications`

Immutable publication header: `id`, owner, monotonically increasing version, status (`staged`,
`published`, `withdrawn`), content hash, created/reviewed/published/withdrawn times, and audit event.
Exactly one active published version per owner.

### `published.portfolio_items`

Allowlisted public snapshot only: publication, stable public ID, source entity type/id for private audit,
section, career stage, display order, title, subtitle, public summary, display metric, display technologies,
sanitized media, public citations, detail slug, and payload schema version. No private description,
source text, embedding, internal URI, confidence, note, or secret may be copied. Anonymous RLS reads only
items in the active publication.

Personal project items include an approved destination state (`detail`, `demo`, or `external`) and may
carry sanitized visual media. Professional project items are emitted as children/references of their
Experience stage rather than as standalone personal-project chapters. Publication metadata contains the
owner-approved profile-rail image/media reference, summary, social/contact links, and availability state.
An owner with no active publication has no synthetic publication row or fallback career items.

### `published.public_evidence`

Minimal citation snapshot: publication, public evidence ID, safe title/issuer/date, sanitized excerpt,
source location label, evidence type, and source-version hash. It deliberately omits private object keys,
raw URLs not approved for display, and surrounding source text.

## Jobs and Opportunity Intelligence

### Sources, Profiles, and Runs

| Table | Purpose and key fields |
|-------|------------------------|
| `app.job_sources` | Owner, name, adapter type/version, provider, enabled state, terms/access note, capabilities, and health status. |
| `app.job_source_configs` | Source, endpoint/connection reference, secret reference, validated headers/parameters/field mappings, rate limits, schedule eligibility, and last test outcome. No plaintext secrets. |
| `app.job_search_profiles` | Owner, name, target titles/synonyms, seniority, locations, work arrangements, contract/employment types, required/preferred/excluded technologies, industries, authorization, compensation, company preferences, scoring weights, timezone, enabled, and optional `archived_at` for safe removal from future searches. |
| `app.automation_schedules` | Owner, purpose, profile/source target, recurrence expression, timezone, jitter, enabled, next run, last run. Consequential actions are not valid purposes. |
| `app.job_search_runs` | Owner, profile, schedule/manual trigger, logical date, status, result counts, start/end, Inngest run/correlation ID, error summary. Unique profile + logical date + trigger type. |
| `app.job_search_run_sources` | Run + source, status, attempts, fetched/accepted/rejected counts, rate-limit metadata, sanitized error. |

### Canonical Jobs

| Table | Purpose and key fields |
|-------|------------------------|
| `app.jobs` | Owner, canonical company/title/location/country, remote/employment/contract types, salary range/currency/period, posted/expiry/discovery dates, status, normalized fingerprint, current description, and optional owner-supplied source URL/provider. |
| `app.job_descriptions` | Job, sequential version, original text hash, normalized text, source, fetched/provided time, language, and active flag. Immutable. |
| `app.job_source_references` | Job, source, external ID, canonical URL, source payload object/hash, first/last seen, source status. Unique source + external ID or canonical URL. |
| `app.job_requirements` | Description version, normalized requirement text, category, priority, skills, sequence, extraction model/prompt version, and confidence. |
| `app.job_requirement_matches` | Requirement, matching run/version, match class, numeric evidence value, explanation, and related claim-evidence links. |
| `app.job_scores` | Job/description, score type (`career_match`, `opportunity`), numeric score, factor values, weights, calculation version, evidence snapshot, and created time. Immutable. |
| `app.job_status_history` | Job, from/to states, actor, reason/note, and transition time. Append-only; transition trigger validates ST-003. |

Deduplication uses unique source identities plus a stored fingerprint and a reviewable similarity result;
automatic merges retain all source references and can be reversed through an audit event.

## Applications and Generated Artifacts

| Table | Purpose and key fields |
|-------|------------------------|
| `app.applications` | Owner, job, optional selected approved application profile, status, started/applied/closed dates, current package, notes, and revision. One active application per owner/job unless explicitly cloned. |
| `app.application_status_history` | Application, from/to state, actor, reason, time. Append-only. |
| `app.application_forms` | Application, source URL, captured time, access method, schema version, status, and source hash. |
| `app.application_fields` | Form, sequence, label, field type, required, choices, char/word limits, category, conditional rule, sensitive flag. Demographic fields default to manual-only. |
| `app.application_answer_versions` | Field/question, version, source (`generated`, `owner`, `adapted`), draft/final text, limit result, prompt/model version, evidence links, owner edits, status, hash, time. |
| `app.saved_answers` | Owner, normalized question category, reusable answer version, tags, allowed contexts, source application, and last reviewed time. |
| `app.application_documents` | Application, owner, document kind, source (`upload`, `link`), private object/source reference, original filename or safe URL label, media type, size, content hash, sequential version, provenance, availability, created time, and removed time. No generated artifact is stored here. |
| `app.application_packages` | Application, sequential version, status, created/reviewed/submitted times, content manifest hash, and owner audit event. |
| `app.application_package_items` | Package + artifact version or analysis snapshot, item type, display order, required flag. Unique package + item identity. |

### `app.generated_artifacts`

Stable artifact identity: owner, application/job, artifact type (`cv`, `cover_letter`, `answer_set`,
`interview_kit`, `application_package`), title, and current version.

### `app.generated_artifact_versions`

Append-only version: artifact, sequence, status from ST-004, structured content JSON with schema version,
template/version, renderer/version, prompt/model/config versions, input and output hashes, private object
keys for PDF/DOCX, size/media type, owner edits, generated/reviewed/final/submitted times, and superseded
version. Final and submitted versions are immutable.

### `app.artifact_evidence`

Artifact version, claim path/span, career fact version, evidence version/chunk, support class, and citation
label. Every material career claim in a generated artifact requires at least one valid row.

## Compensation

| Table | Purpose and key fields |
|-------|------------------------|
| `app.compensation_research` | Owner, job, role/company/location context, strategy, research date, status, confidence, assumptions, and source priority departures. |
| `app.compensation_sources` | Research, source URL/title/provider, observed date, evidence tier, currency, employment model, raw object/hash, and access note. |
| `app.compensation_benchmarks` | Research/source, role/location/company similarity, amount range, period, base/bonus/equity/total type, currency, normalization rate/date, and confidence. |
| `app.compensation_recommendations` | Research, floor/target/stretch, currency/period, recommended answer, strategy, calculation version, and created time. Immutable. |

## Journal and Interview Intelligence

### Journal

| Table | Purpose and key fields |
|-------|------------------------|
| `app.journal_entries` | Owner, title, immutable original text/version, occurred date, entry type, related job/application/interview, private attachment keys, and created time. Corrections create a new owner-authored version; AI never edits it. |
| `app.journal_tags` | Owner + unique normalized tag. |
| `app.journal_entry_tags` | Unique journal entry + tag pair. |
| `app.derived_journal_insights` | Entry/version, insight type, structured value, model/prompt version, confidence, review status, and created time. |

### Interviews

| Table | Purpose and key fields |
|-------|------------------------|
| `app.interview_processes` | Application, source status (`evidence_based`, `manual`, `unknown`), current version, status, and created time. |
| `app.interview_stages` | Process, name, type, sequence, scheduled time/timezone, status, preparation state, outcome, source evidence, confidence, and revision. |
| `app.interview_stage_history` | Stage, prior/new sequence/status/time, actor, reason, and changed time. |
| `app.interview_questions` | Stage/kit/mock session, text, source (`predicted`, `observed`, `manual`), confidence band, rationale, and sequence. Predicted questions cannot use `guaranteed`. |
| `app.interview_topics` | Stage/question, normalized topic/skill, probability band, preparation state, and evidence. |
| `app.interview_preparation_kits` | Stage, generated artifact version, job-description version, company research snapshot, owner notes, and current status. |
| `app.star_stories` | Owner, title, situation/task/action/result, metrics, skills, technologies, project/experience, visibility, and controlling evidence links. |
| `app.question_story_matches` | Question + story, relevance rank, rationale, evidence strength, and run version. |
| `app.mock_interviews` | Owner, application/stage, mode, started/completed times, status, configuration, transcript object key when explicitly retained. |
| `app.mock_interview_feedback` | Mock session, qualitative dimensions, evidence use, strengths, improvements, model/prompt version, and owner review. |

## Blog and Content

| Table | Purpose and key fields |
|-------|------------------------|
| `app.posts` | Owner, stable identity, current version, slug, status, scheduled/published/archived times, and publication approval event. Slug unique. |
| `app.post_versions` | Post, sequence, title, excerpt, content, cover reference, search title/description, prompt/model version where assisted, owner edits, evidence links, hash, and created time. Immutable after publication. |
| `app.tags` | Owner + unique normalized name. |
| `app.post_tags` | Unique post + tag. |

Published post snapshots live with the active portfolio publication. Career-based statements require
artifact evidence; technical discussion remains classified as technical knowledge, not career proof.

## AI, Retrieval, and Evaluation

| Table | Purpose and key fields |
|-------|------------------------|
| `app.ai_provider_configs` | Owner/environment, provider, approved region/data class, secret ref, enabled, timeout/retry/budget, and fallback policy. |
| `app.ai_model_configs` | Provider config, capability, requested model/alias, snapshot model, temperature, token limits, dimensions, and effective dates. |
| `app.ai_task_configs` | Owner + task type, provider/model config, creativity, input/output length limits, timeout, retry policy, ordered fallback configs, enabled state, revision, and effective dates. Unique active owner + task type; configuration changes are audited and secret values are never copied here. |
| `app.prompt_versions` | Immutable prompt name/version/hash, template, variable schema, output schema, author/rationale, status/alias, and supersession. |
| `app.agent_runs` | Owner/public pseudonym, task type, provider/model, prompt version, related entity, status, token/cost/latency, policy result, correlation ID, and sanitized error. No content by default. |
| `app.retrieval_runs` | Agent run, query hash, authorized scope, filter/config version, channel limits, RRF/reranker settings, start/end, and status. |
| `app.retrieval_candidates` | Retrieval run + chunk, lexical/vector ranks and scores, fused rank/score, reranker score, selected flag, and exclusion reason. |
| `app.generation_contexts` | Agent run + opaque evidence handle + immutable chunk ID, exact offsets, and sequence. Only these handles may become citations. |
| `app.generated_claims` | Agent/artifact version, output span/path, normalized claim, support status, and verification result. |
| `app.generated_claim_evidence` | Generated claim + context handle/chunk, support class, validation state, and exact source offsets. |
| `app.evaluation_datasets` | Name, immutable version/hash, visibility, owner, and purpose. |
| `app.evaluation_cases` | Dataset, case ID, input reference/encrypted fixture, expected behavior, labels, and policy tags. |
| `app.evaluation_runs` | Dataset version, application revision, prompt/model/retrieval versions, status, aggregate metrics, start/end. |
| `app.evaluation_results` | Run + case, scorer/version, raw metrics, pass/fail, redacted diagnostics, and reviewer. |

## Analytics, Automation, and Notifications

| Table | Purpose and key fields |
|-------|------------------------|
| `app.analytics_sessions` | Pseudonymous session, consent version, coarse country/device class, started/ended, and retention expiry. No raw IP. |
| `app.analytics_events` | Session/owner, allowlisted event name/version, public content ID, coarse properties, occurred time, retention class. No free text. |
| `app.analytics_daily_metrics` | Owner, date, metric, dimensions, count/value, calculation version. Rebuildable from retained events/lifecycle records. |
| `app.automation_runs` | Owner, schedule/event trigger, purpose, logical key, Inngest ID, status, start/end, attempts, related entity, and sanitized error. Unique purpose + logical key. |
| `app.automation_steps` | Run, step name/version, status, attempt, input/output reference hashes, start/end, and sanitized error. |
| `app.notifications` | Owner, type, related record, channel, payload template/version, status, scheduled/sent/read times, and dedupe key. Informational only. |
| `app.idempotency_keys` | Owner/scope, key, request hash, resource/result reference, status, and expiry. Unique scope + key. |

## Relationships Summary

```text
Active Owner Authorization ── Profile
├── Career Experiences ── Projects ── Achievements ── Metrics
├── Career Facts ── Fact Versions ── Claim Evidence ── Evidence Chunks
│                                              └── Chunk Embeddings
├── Documents ── Document Versions ── Ingestion Runs ── Extracted Facts ── Reviews
├── Projection Rules ── Portfolio Publications ── Portfolio Items/Public Evidence
├── Search Profiles/Sources ── Search Runs ── Jobs ── Requirements/Matches/Scores
│                                               └── Applications
│                                                   ├── Forms/Answers
│                                                   ├── Packages/Artifacts
│                                                   ├── Compensation Research
│                                                   ├── Journal
│                                                   └── Interview Process/Stages/Kits
├── Posts/Post Versions
├── AI/Prompt/Retrieval/Generation/Evaluation Runs
└── Analytics/Automation/Audit
```

## State Transition Enforcement

### Career Fact (ST-001)

- New extraction: `candidate`; owner opening review: `in_review`.
- Terminal review: `approved`, `edited_approved`, or `rejected`; `deferred` may return to `in_review`.
- Only owner or narrowly scoped review function can approve.
- Public eligibility additionally requires `visibility = public` and an active projection rule.

### Document Ingestion (ST-002)

- `pending -> running -> completed|partial|failed|cancelled`; cancellation is allowed before another
  terminal state and uses the shared `run_status` vocabulary.
- A changed hash creates a new immutable version. A removed or inaccessible source creates a tombstone.
- Verified facts are not cascade-deleted; their source availability and publication eligibility update.

### Job (ST-003)

- Transition function validates allowed forward/backward edges and always appends history.
- Terminal jobs may reopen only with owner action or a new source version and an audit reason.
- Application and interview states do not infer a submitted action without owner confirmation.

### Artifact (ST-004)

- `draft -> owner_reviewed -> final -> submitted_snapshot`; any non-submitted version may be superseded.
- Final and submitted content is immutable. Editing creates a new draft version.

### Interview Stage (ST-005)

- `proposed|manual -> planned -> scheduled -> completed|cancelled|skipped`.
- Owner may correct or reorder through append-only history; inferred stage confidence never grants action.

### Article (ST-006)

- `draft -> scheduled|published -> archived`; scheduled may return to draft before publication.
- Publishing and republishing require a recorded owner approval and immutable published version.

### Automation Run (ST-007)

- `pending -> running -> completed|partial|failed|cancelled`.
- Retries create step attempts under the same logical run and idempotency key.

## Validation and Integrity Rules

- Every public item must reference an approved source entity and active publication; a publish function
  rejects private/restricted evidence, missing claim support, unsafe URLs, and confidential fields.
- Only the active `app.owner_authorizations` subject may own private records or create an owner profile;
  RLS and negative tests deny authenticated subjects without that authorization.
- Experience publication validates the six stable stage keys, display order, capability/title semantics,
  and child relationships for professional projects, impact, metrics, skills, and tools.
- Personal projects marked public/featured require an approved detail, demo, or external destination;
  unavailable destinations fail publication validation rather than rendering a false working link.
- Every generated material career claim must have at least one valid `artifact_evidence` or be explicitly
  classified as owner intent rather than career fact.
- Trust and visibility may become stricter automatically; making them less strict requires owner review.
- Model confidence never updates trust level or review status directly.
- Requirement match and opportunity score functions accept versioned factor inputs and store a complete
  calculation snapshot. AI has no update privilege on score columns.
- Journal originals, source bytes, fact versions, published versions, submitted artifacts, audit events,
  and state histories are append-only.
- Slugs and public identifiers are normalized, unique, and cannot expose internal sequential IDs.
- External URLs allow HTTPS by default and pass the SSRF-aware fetch policy before server retrieval.
- Retention jobs may purge raw transient payloads and telemetry after policy expiry but preserve minimal
  hashes, version identity, state history, and legal/audit metadata when required.

## Index and Query Plan

- B-tree every foreign key and common owner/time/status predicate.
- Partial indexes for active jobs, pending reviews, enabled schedules, active publications, and nondeleted
  chunks.
- GIN indexes for weighted chunk full-text vectors and bounded JSONB fields used in owner filters.
- Exact vector scan first; add per-model HNSW cosine expression index after benchmark threshold.
- Unique indexes enforce external source IDs, content versions, idempotency keys, score versions, slugs,
  publication version, and active owner/job application constraints.
- Partition high-growth `analytics_events`, `audit_events`, and run/step tables by time only after volume
  measurements justify it; do not pre-partition small career tables.

## Migration, Rollback, and Recovery

1. Migrations are ordered SQL in `supabase/migrations` and include constraints, indexes, RLS, grants,
   security-invoker views/functions, and pgTAP allow/deny tests.
2. Prefer expand/migrate/contract changes. Deploy readers for both shapes before backfill and removal.
3. Backfills are idempotent, checkpointed, rate-limited, and observable through automation runs.
4. Destructive schema changes require a verified backup, forward-recovery migration, and owner approval.
5. Publication snapshots and generated artifacts are rebuildable from immutable versions, except owner
   edits and submitted binaries, which are backed up as protected originals.
6. Restore drills verify private object references, auth subject mapping, RLS, publication selection,
   evidence links, and exact submitted artifact retrieval.
7. The owner-authorization migration is expand-first: create the authorization relation and deny
   non-authorized bootstrap, insert the reviewed existing owner subject, update profile/RLS predicates,
   run owner/non-owner negative tests, then remove any legacy authenticated-user-equals-owner behavior.
   Recovery restores the reviewed authorization row from protected configuration/audit evidence; it does
   not infer a new owner from the most recent sign-in.
